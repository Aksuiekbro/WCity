{
  "name": "wcity",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently -n api,web -c green,blue \"PORT=3001 npm run start:dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "npm run start:prod --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}


